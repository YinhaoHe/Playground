const numbers = [1, 2, 3, 4];

// Add
// numbers.push();
// numbers.unshift();
// numbers.splice();

// Remove
// End
const last = numbers.pop();

// Beginning
const first = numbers.shift();

// Middle
numbers.splice(2, 1);
// output 1, 2, 4
numbers.splice(2, 2);
// output 1, 2
